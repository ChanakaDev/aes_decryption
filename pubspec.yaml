name: aes_decryption
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  crypto: ^3.0.3
  cupertino_icons: ^1.0.2
  encrypt: ^5.0.1
  flutter:
    sdk: flutter
  hex: ^0.2.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
